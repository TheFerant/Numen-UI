import { Layout, Playground, Attributes } from "lib/components";
import { Text } from "@numen-ui/core";

export const meta = {
  title: "Text",
  group: "General",
  index: 10,
};

## Text

Display text using well-defined typographic styles.

<Playground
  title="Headings"
  desc="Use `Text` to standardize text content."
  scope={{ Text }}
  code={`
<Text as="h1">Start our Numen journey.</Text>
`}
/>

<Playground
  title="Paragraph"
  desc="Add `p` and `b` attributes to a `Text`."
  scope={{ Text }}
  code={`
<>
  <Text as="p">
   Caching is very important for fast Web sites. This article describes different methods of caching and how to use HTTP Headers to control them.
  </Text>
  <Text as="b">
   A brief description of the changes between the early versions of HTTP, to the modern HTTP/2, the emergent HTTP/3 and beyond.
  </Text>
</>
`}
/>

<Playground
  title="Small"
  scope={{ Text }}
  code={`
<>
  <Text as="p" sx={{fontSize: "12px"}}>
   HTTP response codes indicate whether a specific HTTP request has been successfully completed. Responses are grouped in five classes: informational responses, successful responses, redirections, client errors, and servers errors.
  </Text>
  <Text as="p" sx={{fontSize: "12px", fontStyle: "italic"}}>
   HTTP response codes indicate whether a specific HTTP request has been successfully completed. Responses are grouped in five classes: informational responses, successful responses, redirections, client errors, and servers errors.
  </Text>
</>
`}
/>

<Playground
  title="Blockquote"
  scope={{ Text }}
  code={`
<Text as="blockquote">
  Send cookies from the server to the user-agent.
</Text>
`}
/>

<Playground
  title="Sizes"
  scope={{ Text }}
  code={`
<>
  <Text as="p" sx={{fontSize: "12px", marginTop: 0}}>Font Size: 12px;</Text>
  <Text as="p" sx={{fontSize: "14px"}}>Font Size: 14px;</Text>
  <Text as="p" sx={{fontSize: "1rem", marginBottom: 0}}>Font Size: 1rem;</Text>
</>
`}
/>

<Playground
  title="Compose"
  desc="Effect of multiple `Text` stacks"
  scope={{ Text }}
  code={`
<>
  <Text as="p" sx={{marginTop: 0}}>
   Specifies origins that are allowed to see values of attributes retrieved via features of the Resource Timing API, which would otherwise be reported as zero due to cross-origin restrictions.
  </Text>

  <Text as="p" sx={{marginBottom: 0}}>
    <Text as="strike" sx={{fontSize: "12px", marginTop: 0}}>Indicates if the resource transmitted should be displayed inline (default behavior without the header), </Text>
    <Text as="span" sx={{fontSize: "12px", fontWeight: "bold"}}>or if it should be handled like a download and the browser should present a “Save As” dialog.</Text>
  </Text>
</>
`} />

<Attributes edit="/pages/en-us/components/text.mdx">
<Attributes.Title>Text.Props</Attributes.Title>

| Attribute | Description    | Type      | Accepted values | Default |
| --------- | -------------- | --------- | --------------- | ------- |
| **as**    | The underlying element to render — either a HTML element name or a React component. | [React.ElementType](https://github.com/DefinitelyTyped/DefinitelyTyped/blob/master/types/react/index.d.ts#L73) | -               | `span`  |
| **sx**    | Style overrides to apply to the component. See also overriding styles. | [SystemStyleObject](https://github.com/DefinitelyTyped/DefinitelyTyped/blob/master/types/styled-system__css/index.d.ts#L407) | -               | - |

</Attributes>

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;
